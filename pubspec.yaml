name: flutter_onboarding
description: Actively maintained, community driven flutter animated and onetime onboarding project.
version: 0.0.6
homepage: https://github.com//Iamkrishnaa/flutter_onboarding
environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  assets:
    - assets/
